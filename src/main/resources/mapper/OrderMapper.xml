<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rentcar.mapper.OrderMapper">
    <select id="selectPage" resultType="com.rentcar.bean.Order">
        select co.*, ci.plate_number car_plate_number, cus.username customer_name
        from car.car_order co
        left join car.customer cus on cus.id = co.customer_id
        left join car.car_info ci on ci.id = co.car_info_id
        <if test="ew.customSqlSegment != '' and ew.customSqlSegment != null">
            ${ew.customSqlSegment}
        </if>
    </select>
</mapper>